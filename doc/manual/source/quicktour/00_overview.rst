Rotonda Overview
================

So, as said earlier, Rotonda can be though of as a pipieline through which BGP
data will flow. This pipeline consists of units that are connected to each other through gates.