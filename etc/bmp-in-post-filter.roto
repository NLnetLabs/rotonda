filter bmp-in-post-filter {
    define {
        rx msg: BmpMessage;
    }

    term dummy {
        match {
            1 == 1;
        }
    }

    apply {
        filter match dummy matching {
             return accept;
        };
        accept;
    }
}
