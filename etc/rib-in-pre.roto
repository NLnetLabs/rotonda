filter rib-in-pre-filter {
    define {
        rx msg: Route;
    }

    term dummy {
        match {
            1 == 1;
        }
    }

    apply {
        filter match dummy matching {
             return accept;
        };
        accept;
    }
}
